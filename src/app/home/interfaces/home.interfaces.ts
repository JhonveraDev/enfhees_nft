export interface recentUser {
  name: string;
  items: number;
  elements: number;
  views: number;
  avatar: string;
}

export interface Image {
  id: number;
  title: string;
  category: string;
  url: string;
}